import { Meta, Canvas, Story } from "@storybook/addon-docs";
import { Button } from '../components/Button';

<Meta title="Documentation/Bem Vindo" />


# Bem vindo!
### Storybook Catapulta

export const Template = (args) => <Button {...args } />;

<Canvas>
  <Story 
    name="Button Primary"
    args={{
      primary: true, 
      label: 'Entrar',
      onClick: () => alert('clicou')
    }}>
    {Template.bind({})}
  </Story>
  <Story 
    name="Button Secondary"
    args={{
      primary: false, 
      label: 'Entrar',
      onClick: () => alert('clicou')
    }}>
    {Template.bind({})}
  </Story>
</Canvas>